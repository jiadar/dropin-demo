api: Runs the api-examples.rb which executes sample api calls for all implemented APIs
load: Loads the fixture data into the current environment database
purge: Drops the database, runs all migrations, and loads fixtures
reset: Rolls back the database, runs all migrations, and loads fixtures
